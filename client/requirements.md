## Packages
framer-motion | For smooth page transitions and hover animations
react-icons | For social media and UI icons (using Lucide in base stack but react-icons covers specific brand logos better if needed, sticking to Lucide for consistency with base stack per constraints if possible, but framer-motion is key)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
